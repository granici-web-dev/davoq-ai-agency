# Находки gitleaks, разобранные вручную. Каждая строка — отпечаток
# «коммит:файл:правило:строка» и причина, по которой это не утечка.
#
# Пустой файл лучше строки без причины: подавление, о котором никто не помнит,
# почему оно стоит, — это выключенная проверка, притворяющаяся включённой.

# Публичный ключ виджета `pk_…` в снимке страницы Playwright. Ключ публичный
# по устройству: он стоит в теге <script> на сайте каждого клиента и виден
# любому посетителю. Права он не даёт никаких — только показать виджет
# с разрешённого домена. Сам снимок из дерева удалён, а .playwright-mcp/
# с тех пор в .gitignore.
447d8e00aaaadf060e0fe77848f4cc655546fd6a:apps/engine/.playwright-mcp/page-2026-08-21T14-00-12-937Z.yml:generic-api-key:24
