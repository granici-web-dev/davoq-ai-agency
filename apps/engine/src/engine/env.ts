/**
 * Чтение .env. Вынесено отдельным модулем и импортируется ПЕРВЫМ в точках входа.
 *
 * Причина: в ESM все импорты выполняются до тела модуля. Вызов loadEnvFile()
 * внутри файла срабатывает уже после того, как импортированный модуль создал
 * пул подключений с пустым DATABASE_URL — и падает не там, где ошибка,
 * а глубоко внутри драйвера, сообщением «client password must be a string».
 */
try {
  process.loadEnvFile();
} catch {
  /* .env необязателен: в проде переменные приходят из окружения */
}
