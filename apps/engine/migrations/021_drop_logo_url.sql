-- Старая колонка с путём к файлу в каталоге сборки больше не используется:
-- логотип живёт в хранилище тенанта (миграция 020), а адрес в панели
-- собирается из наличия ключа и не содержит имени файла.
BEGIN;

ALTER TABLE tenants DROP COLUMN logo_url;

COMMIT;
