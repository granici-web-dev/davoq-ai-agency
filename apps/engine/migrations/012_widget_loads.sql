-- Показы виджета.
-- Без них «15 заявок за две недели» нечем измерить: неясно, виджет ли плох
-- или на сайт просто никто не заходит. Счётчик считает загрузки конфигурации —
-- виджет запрашивает её один раз на просмотр страницы.
BEGIN;
ALTER TABLE usage_daily ADD COLUMN widget_loads integer NOT NULL DEFAULT 0;
COMMIT;
