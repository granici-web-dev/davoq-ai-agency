-- Логотип тенанта в шапке панели. Колонка, а не файл в сборке: продукт
-- белолейбловый, и логотип клиента, вшитый в бандл, пришлось бы выкорчёвывать
-- при первом же втором клиенте.
ALTER TABLE tenants ADD COLUMN logo_url text;
