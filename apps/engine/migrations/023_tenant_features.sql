-- Набор экранов панели — на тенанта.
--
-- Раньше два экрана были скрыты константой в сборке: решение принималось
-- для пилотного клиента, а действовало на всех. Второму клиенту с CRM
-- коннекторы понадобятся, и вернуть их можно было только пересборкой движка.
BEGIN;

-- Пусто — значит «всё, что даёт тариф». Список, а не набор флагов на колонку:
-- экраны добавляются чаще, чем стоит менять схему.
ALTER TABLE tenants ADD COLUMN hidden_screens text[] NOT NULL DEFAULT '{}';

-- Существующим сохраняем ровно то, что они видели вчера.
UPDATE tenants SET hidden_screens = ARRAY['kb', 'connectors'];

COMMIT;
