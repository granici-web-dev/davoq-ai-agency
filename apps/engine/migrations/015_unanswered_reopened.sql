-- Отметка о возврате вопроса в список.
--
-- Нужна ради одной строки в панели: директор убрал устаревший файл — и должен
-- увидеть, что вместе с ним из ответов бота исчезла тема. Без этого удаление
-- выглядит бесплатным, а список молча отрастает обратно.
ALTER TABLE unanswered_log ADD COLUMN reopened_at timestamptz;
