-- Отправитель писем о заявках — на тенанта.
--
-- Раньше адрес брался из MAIL_FROM, одной переменной на процесс. На одном
-- клиенте это незаметно; на втором его отдел продаж начинает получать письма
-- от имени первого — и эти письма не проходят проверку домена получателя,
-- то есть уезжают в спам ровно тогда, когда важнее всего.
BEGIN;

ALTER TABLE tenants ADD COLUMN lead_notify_from text;

COMMIT;
