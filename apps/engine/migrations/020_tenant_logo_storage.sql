-- Логотип клиента — в хранилище тенанта, а не в каталоге сборки движка.
--
-- Раньше файл клали руками в dist/brand/ и записывали путь в logo_url. При
-- десяти клиентах это десять ручных операций и десять чужих файлов, которые
-- пересобираются вместе с движком. Брендинг — данные клиента, и лежать он
-- должен там же, где остальные его файлы: под префиксом тенанта.
BEGIN;

ALTER TABLE tenants
  ADD COLUMN logo_key  text,
  ADD COLUMN logo_mime text;

COMMIT;
