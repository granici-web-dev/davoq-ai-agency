{
  "name": "assistwidget-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "site": "npm run dev -w @assistwidget/site",
    "engine": "npm run dev -w @assistwidget/engine",
    "contract": "npm run generate -w @assistwidget/contract",
    "contract:check": "npm run check -w @assistwidget/contract",
    "typecheck": "npm run typecheck -w @assistwidget/engine && npm run -w @assistwidget/site exec -- tsc --noEmit",
    "build": "npm run build -w @assistwidget/contract && npm run build -w @assistwidget/engine && npm run build -w @assistwidget/site"
  },
  "engines": {
    "node": ">=22"
  }
}
