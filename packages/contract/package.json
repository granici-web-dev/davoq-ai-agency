{
  "name": "@assistwidget/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "node build.mjs && tsc -p tsconfig.json",
    "check": "node build.mjs --check",
    "compile": "tsc -p tsconfig.json",
    "build": "npm run check && npm run compile"
  },
  "dependencies": {
    "yaml": "^2.0.0"
  }
}
